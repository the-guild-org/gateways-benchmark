FROM rust:1.72.0 as builder

WORKDIR /usr/src/subgraph
COPY Cargo.toml Cargo.lock subgraph.rs ./
RUN cargo install --path .

FROM debian:12.1
COPY --from=builder /usr/local/cargo/bin/subgraph /usr/local/bin/subgraph

CMD subgraph
